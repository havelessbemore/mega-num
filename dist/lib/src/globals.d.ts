export declare module Globals {
    const MIN_BASE: number;
    const MAX_BASE: number;
    const MAX_PRECISION: number;
    let DEFAULT_IS_MUTABLE: boolean;
    let DEFAULT_BASE: number;
    let DEFAULT_CIPHER: ReadonlyArray<string>;
}
